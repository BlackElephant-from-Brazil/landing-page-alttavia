# Privacy notice for /en/privacy: proposal

**Status:** PROPOSAL for Patrícia Viana to correct and approve by **Wednesday, September 23, 2026**; Thursday, September 24, 12:00 Lisbon time, is the last slot. Not wired into any page. Written on September 21, 2026 from the code on branch `main-split-bank-and-nif`, and reviewed again the same day after that day's changes (commit `e3c5fbb`).

This is a factual draft prepared by the developer for the lawyer's review. It is not legal advice from us.

How to read it:

- **[TO CONFIRM]** marks an assumption, a fact outside the code, or a legal decision (controller, transfers, safeguards, exemptions) that must be settled before publishing. The legal decisions are the firm's, and each one names its item in the list at the end of `fatos-para-patricia.md` where it can. The technical facts (a provider's region or log period, a setting in a provider's dashboard, the developer's business name and country) are the developer's to check and fill in; the firm does not need to act on them.
- **PROPOSAL** marks a value or a legal choice only the firm can decide. Retention periods and legal bases are all PROPOSAL.
- Everything else describes what the platform does today. The source of each statement is listed in the last section.
- The section "The notice" is the page as it would be published: US English, house rules for copy, short sentences.
- Once approved, `/en/privacy` should be linked from the landing footer (today its "Privacy" link goes to `https://alttavia-relocation.com/en/privacy-policy`, the main site's general policy, which does not describe this platform) and from the client area. That footer also shows on the application form, the success page and `/en/login`. The client area links no privacy text today, although the details form and the document uploads collect the most data; place the link next to them. The purchase drawer links only the service terms.
- The same footer carries the legal identity, on every page built with `LegalPage` too, so `/en/privacy` would show it. It reads "Alttavia Relocation · Viana Consultancy · NIPC 518 856 984", prints Av. António Augusto Aguiar 24, 1º direito, 1050-016 Lisboa, and ends "© {year} Viana Consultancy. All rights reserved." (`brand.legalEntity` and `brand.address` in `src/content/brand.ts`). The landing's JSON-LD gives search engines `legalName: "Viana Consultancy"` (`src/components/bank/structured-data.tsx`). Before this page goes live, both must name the controller chosen in "Who we are".
- These changes are outside this file.

## The notice

# Privacy notice

Last updated: [publication date]

This notice covers the personal data we collect through this site and your client area. It explains why we need it, who handles it and how long we keep it. It also explains what you can ask of us.

### Who we are

ALTTAVIA RELOCATION, Unipessoal Lda., NIPC 518 856 984, with registered office at [TO CONFIRM: address], Portugal.

[TO CONFIRM, the name and the address, against the company's certidão permanente. The service agreement and its Part C withdrawal form give Av. Elias Garcia, 123-A, 1050-098 Lisbon as this company's registered office, the only one the code gives for it. The powers of attorney give Av. António Augusto Aguiar, 24, 1.º Direito, Escritório 3, 1050-016 Lisbon as Patrícia Soares Viana's professional address as attorney, and the Word letterhead, which the PDF does not print, also shows it. The site footer prints the Aguiar address next to "Viana Consultancy" and this NIPC, and the landing's data for search engines gives the legal name "Viana Consultancy". The Stripe account that takes the payment still shows the public name "Consulting". All of them must name the same company. Which address is the registered office is asked once, in the contract samples message, point 4.]

We decide how your data is used. Under the GDPR, that makes us the controller. [TO CONFIRM, a legal choice for Patrícia: who the controller is. The company signs the service agreement. The legal services are rendered by Attorney Patrícia Soares Viana through the company, on an independent basis (First Clause, 2), and the powers of attorney appoint her personally as attorney and, for a NIF, as tax representative. Options: the company alone; the company and Patrícia Soares Viana as separate controllers, each for its own part; joint controllers. This sentence and "Who receives your data" change with the answer.]

For anything in this notice, write to info@alttavia-relocation.com. [TO CONFIRM: no data protection officer is named today. The firm decides whether one is required.]

### What we collect and why

**The application form.** The country on your proof of address, how many adults are applying, whether children also need a NIF (yes or no, nothing else), whether each adult already has a NIF, the bank account you want, the country of each passport, and your visa type. Until you confirm your sign in code and your order is saved, these answers stay in your browser tab. We use them to suggest a service and prepare your order. Legal basis (PROPOSAL): steps you ask for before a contract (GDPR Article 6(1)(b)).

**Your account.** Your first name and your email address. Your account is created when you ask for your first sign in code, even if you never enter it. We use your name and email to open your client area and send your sign in codes. Legal basis (PROPOSAL): contract (Article 6(1)(b)).

**Your payment.** The service, the price, the payment date and Stripe's payment references. We give Stripe your email address and our order and account references, so the payment matches your order. You enter your card details on Stripe's page. We never see or store them. Legal basis (PROPOSAL): contract, and our legal duty to keep accounting and tax records (Article 6(1)(c)).

**Your documents.** Depending on the service: passport, proof of address, the tax number from your country, bank statements or an annual income statement, proof of your profession, your Portuguese NIF document (if you buy the Bank Account only service), and the powers of attorney you sign. We use them to apply for your NIF or open your account. Legal basis (PROPOSAL): contract. [TO CONFIRM: also a legal obligation, if Portuguese anti money laundering law applies to the bank account service.]

**Your details for the documents we prepare.** Full name, whether the documents should refer to you as she or he, place and date of birth, passport number, issuing authority, issue and expiry dates, tax residence address and, if you give it, the city and country you are in when you confirm them. We print them on your powers of attorney. For services that come with a service agreement, we also print them on it, except whether we refer to you as she or he. The city and country you give appear only on the agreement. We keep a copy of the agreement as it was prepared. Legal basis (PROPOSAL): contract. [TO CONFIRM: fatos item 15. If the platform launches without agreements, drop every mention of the service agreement from this notice.]

**What we deliver.** Your NIF document, your Portal das Finanças access, your IBAN confirmation and a closing report. For a bank account, the bank may also give us cards, access codes or online banking credentials for you, and we pass them on. [TO CONFIRM, fatos item 17: how they reach you, and whether we keep a copy. The platform has no delivery slot for them.] Legal basis (PROPOSAL): contract.

**Your order history.** We record each step of your order: when it happened and who on our team made it. For each file you upload we keep its name, type and size, when you sent it, and our review. When we ask for a new file, we keep the reason and the earlier file too. We use this to run your order and to show you where it stands. Legal basis (PROPOSAL): contract.

**Details about your partner.** Some orders cover your partner. This happens when you apply together, when you buy the Couple package, or when you buy a second NIF for them from your client area. When you apply together, you give us their answers to the form. For any order that covers them, you give us their documents and their details for the documents we prepare. We use them in the same way and keep them for the same time. Everything about that order reaches you, through your account and your email. Please show them this notice before you share their data. Legal basis: [TO CONFIRM: none is proposed yet, fatos item 3]. [TO CONFIRM: how the firm informs the partner, who has no account of their own (GDPR Article 14).]

**Details about your children.** [TO CONFIRM, the whole paragraph, fatos item 11.] The form asks only whether your children also need a NIF. Children's NIFs are arranged on request, outside this platform: you tell us how many on WhatsApp and we quote them separately. [TO CONFIRM: the form's note says "we add them to the order", which an order on this platform cannot hold (two adults at most, no child slots). Also what the firm collects for a child's NIF and through which channel, who gives it and signs for the child, the legal basis, and how long it is kept.]

**Messages.** Emails between us, and WhatsApp messages if you choose to write to us there. If you write to us on WhatsApp, WhatsApp carries and stores those messages under its own terms. [TO CONFIRM: name WhatsApp only here, or also as a row in the providers table below.] Legal basis (PROPOSAL): contract, or steps before a contract.

**Technical records.** Our host receives the IP address and browser of every visit. Our database and sign in provider, and our file storage, also receive them when your browser signs in, uploads a file or downloads one. Our database keeps the IP address and browser of each sign in. We keep a record each time our team opens, reviews or changes a file or a step of your order: who, what and when. Our server logs can include your email address or your account ID. When our team writes a note about how the platform works, the note records the screen address, which can include your email address or the reference of your order. We use these records to keep the service secure and working. Legal basis (PROPOSAL): our legitimate interest (Article 6(1)(f)).

We do not sell your data and we do not use it for advertising.

A person, not a computer, makes every decision about you. [TO CONFIRM, a legal judgment for Patrícia (GDPR Article 22, fatos item 3): whether the form's suggestion below is outside that article.] The form uses your answers, including the country of each passport and your visa, to suggest a service. If you still need a NIF and your answers make a bank account unlikely, the form tells you and recommends the NIF first. [TO CONFIRM: kept narrow on purpose, see `service-terms-changes.md` 1.5. When you already have a NIF, the form shows no such warning and says there is nothing to order; and when you still need one, a package with the account (NIF + Bank Account, or the Couple package) stays listed as another option.] You choose what to buy, you can still write to us, and our team reviews every document.

We do not rely on your consent for anything today. [TO CONFIRM: depends on fatos item 3 and on the agreement's Sixteenth Clause, 2, where the client "expressly authorises" the processing.] If we ever add analytics or marketing, we will ask first, and you can withdraw that consent at any time.

The details form in your client area accepts only people aged 18 or over.

### Who receives your data

- **Finanças** (Autoridade Tributária e Aduaneira), for your NIF, and **the bank** that reviews your account opening. Each acts under its own legal duties.
- **Patrícia Soares Viana**, attorney at law. Your powers of attorney appoint her to act for you before Finanças and the bank and, for a NIF, as your tax representative, so she receives Finanças notifications for you. [TO CONFIRM: listed here as a recipient, or named in "Who we are" as a controller (fatos item 1 a); who the tax representative is, since the service agreement has the company accept that role (fatos item 1 e); and whether house rule 2 of the landing, which keeps her credential to one section of that page, applies to this page.]
- **Our team**, bound by professional confidentiality. [TO CONFIRM, fatos item 9: who at the firm is bound by professional secrecy, and on what basis for staff who are not lawyers and for the developer's support account below.] [TO CONFIRM: two administrator accounts exist today, and both see every order and can open every file. One is Patrícia's (info@alttavia-relocation.com). The other, since September 21, 2026, is a support account of the developer guyshore.com (business+admin@guyshore.com), used for tests and support; it belongs to the developer, not to the firm's team, see the guyshore.com row below. List who else at the firm will have access, and decide whether the support account keeps admin rights after launch.]
- **The service providers below.** Except where this notice says otherwise, they handle data only on our instructions. [TO CONFIRM, for Patrícia, fatos item 7: which of them act only on our instructions and which also act for their own purposes, as Stripe does (see below). Also the contract chain. None of these accounts is the company's: the Supabase project is in the organization of guyshore.com, the R2 bucket in the developer's own Cloudflare account, and the Netlify site in the developer's team guiblackelephant. The legal person that holds each account (guyshore.com, the developer personally, or another business) is not recorded. Either the accounts move to the company, or the data processing agreement with whoever holds them (GDPR Article 28) names these providers as its subprocessors. Who holds the Resend account is not recorded either.]

### Service providers and where your data is stored

| Provider | What it does for us | Where the data is |
|---|---|---|
| Supabase | Database and sign in: your account, answers, orders and their history, your details, our reviews and closing report, and the records of your documents and agreement | London, United Kingdom |
| Cloudflare R2 | File storage: your documents, signed powers of attorney, service agreements and the files we deliver | European Union |
| Resend | Sends our emails. To you: sign in codes, password recovery codes, order updates and the reason when we ask for a new document. If your service comes with a service agreement, it goes to you as a PDF attachment, with your passport and birth details. To our team: notices with your email address, the service, the amount and the order reference | [TO CONFIRM: the region set for the sending domain, and how long Resend keeps each email and its attachment] |
| Stripe | Takes your payment. We send it your email address, the service and our order and account references. You give your card details to Stripe directly | [TO CONFIRM: the company that holds our Stripe account (its public name still reads "Consulting") must be the one named in "Who we are"; and Stripe's contracting entity, for example Stripe Payments Europe, Limited, Ireland] |
| Netlify | Hosts this site, runs its server code and keeps its server logs | [TO CONFIRM: region of the server functions and how long the logs are kept] |
| [TO CONFIRM: provider of our mailbox at alttavia-relocation.com] | Holds the emails you send us, your replies to ours, and our team's notices about your order | [TO CONFIRM] |
| guyshore.com [TO CONFIRM: legal name of the business] | Builds and maintains the platform. The Supabase project above is in its organization. The Cloudflare account and the Netlify team above belong to its developer [TO CONFIRM: the legal person that holds each]. It can reach the data in all three directly. It also has access to our Stripe account. It has an administrator account on this platform for tests and support. It keeps backup copies of the database on its own computer. These copies can include your files | [TO CONFIRM: country of the business and of the computer that holds the backups] |

Stripe also uses some payment data for its own purposes, such as fraud prevention, under its own privacy policy. [TO CONFIRM]

Some of these companies are based outside the European Union. [TO CONFIRM, for Patrícia, for each provider, for guyshore.com and for WhatsApp: the country of the company that handles the data, which is not always where it is stored (Supabase stores in London, R2 in the EU); whether that is a transfer; and the safeguard, for example an adequacy decision (for the United Kingdom, the one in force on the publication date), certification under the EU and US Data Privacy Framework, or the Standard Contractual Clauses.] Ask us and we will send you a copy of the safeguards.

### How long we keep it

Nothing in our platform deletes your data automatically today. Every deletion is made by hand. The periods below are PROPOSAL values for the firm to decide.

- **An account that never pays:** your email, your first name, your answers and the unpaid order. Kept for PROPOSAL 12 months after your last sign in, or after the account was created if you never signed in, then deleted. This also covers an email that asked for a sign in code and went no further.
- **Your documents, your details for the documents, and the files we deliver:** kept until your service ends, plus PROPOSAL [N] months (for example 12), then deleted. [TO CONFIRM: do not tie N to the tax representation. Its twelve months are still open in the contract samples message, point 2, and a Bank Account only order has no tax representation.] [TO CONFIRM: when a withdrawn, closed or refused order "ends", since the platform marks only completed ones; and whether anti money laundering law or professional rules require longer, as the Sixteenth Clause, 3 of your agreement allows.]
- **Files you replaced and earlier versions of your agreement:** deleted PROPOSAL with the rest of your documents. [TO CONFIRM: or sooner]
- **Your service agreement, and the payment and order records of your paid orders:** kept for PROPOSAL 10 years, the period Portuguese law sets for accounting records. [TO CONFIRM] Your agreement shows your name, birth details, passport details and tax address, so these stay with it for the same period.
- **Backup copies of our database:** kept for PROPOSAL [N] days, then deleted. Data we delete from the platform stays in older backups until those are deleted.
- **Emails:** we keep our copies for as long as we keep the order they belong to (PROPOSAL). You keep your own copies. Our email provider keeps a record of each message for its own period. [TO CONFIRM: Resend's period on our plan, and whether it keeps attachments such as your service agreement.]
- **Technical records:** the sign in records in our database are kept for PROPOSAL [N] months. Server logs are kept as long as our hosting, database and file storage providers keep them. [TO CONFIRM: the Netlify, Supabase and Cloudflare log periods on our plans.] Our team's notes about the platform are kept for PROPOSAL [N] months. [TO CONFIRM: today nothing deletes them, fatos item 5.]

PROPOSAL: When a period ends, we delete the data or make it anonymous, in our systems and in our backup copies. [TO CONFIRM: no tool does this today. The developer would do it by hand. Delete or anonymize is fatos item 6; backups are fatos item 10. Publish this sentence only once that process is agreed.]

### Your rights

You can ask us to:

- show you the data we hold about you and send you a copy;
- correct it;
- delete it, unless a law requires us to keep it;
- limit how we use it, or stop a use based on our legitimate interest;
- send you the data you gave us in a common format.

Write to info@alttavia-relocation.com from the email address on your account. We answer within one month. You can correct the details for your documents yourself in your client area, until you upload the signed powers of attorney. If your service agreement was already prepared, write to us and we send you a corrected one. You can download your files, your service agreement if your service has one, and what we deliver at any time. To change your email address, delete a file or close your account, write to us.

### Complaints

You can complain to the Portuguese data protection authority, the Comissão Nacional de Proteção de Dados (CNPD), at www.cnpd.pt, or to the data protection authority of the EU country where you live or work. [TO CONFIRM] We would like the chance to help first, so please write to us too.

### Cookies and browser storage

This site sets only what it needs to work. There are no analytics or advertising cookies.

| Name | What it does | How long |
|---|---|---|
| `sb-dgdbrnvgrpixsslgvmns-auth-token` (sometimes split into `.0` and `.1`) | Keeps you signed in to your client area | Removed when you sign out; otherwise your browser keeps it up to 400 days |
| `sb-dgdbrnvgrpixsslgvmns-auth-token-code-verifier`, `sb-dgdbrnvgrpixsslgvmns-auth-token-flows-code-verifier` and `sb-dgdbrnvgrpixsslgvmns-auth-token-flow-[id]-code-verifier` | Used when we send you a sign in code | Removed when you sign out; otherwise your browser keeps them up to 400 days [names seen in a browser on staging on 21/09; lifetime TO CONFIRM] |
| `alttavia_apply_v1`, `alttavia_apply_checkout_v1`, `alttavia_apply_name_v1` (session storage, not cookies) | Your form answers, the service you chose, the email address we sent your code to, and your first name, until your order is saved | Removed when your order is saved or when you close the tab |

Fonts are served from this site, so your browser does not contact Google for them. The payment page is Stripe's own site, under Stripe's cookie policy.

Because all of the above is strictly necessary, we do not ask for consent to it. [TO CONFIRM] The site can load Google Tag Manager for analytics. It is switched off today. Before we switch it on, we will add a banner that asks for your consent first.

### Security

Every page and every upload uses an encrypted connection. Your files sit in private storage. You see only your own orders. The files you upload and the files we deliver open through links that expire after two minutes. Your service agreement and the powers of attorney we prepare open only in your client area, while you are signed in. Our team can open all of them in the admin area. So can the developer who maintains the platform, who can also reach the storage directly and keeps backup copies (see the guyshore.com row above). Our team signs in to the admin area with a password. [TO CONFIRM: once two step sign in is enforced on September 24, 2026, write "with a password and a second step", and add "Access to an email inbox alone never opens the admin area." Until then, anyone who reads an admin's inbox can reset the password.] For services that come with a service agreement, we also send it to your email address as an attachment. [TO CONFIRM: public access is off on the storage bucket, a Cloudflare setting the code does not record.]

### Changes

When what we do changes, we update this notice and the date at the top.

## Where each statement comes from

For the reviewer. Paths are relative to the repository root.

- **Form answers.** `src/lib/apply/types.ts` (`Answers`: `residence`, `applicants`, `childrenNifs` as a boolean, `hasNif`, `bank`, `passport`, `visa`). Before sign in they live only in sessionStorage (`src/lib/apply/storage.ts`, `src/components/apply/checkout-storage.ts`). `POST /api/apply/submit` writes them to `public.user_answers` and `user_services.answers_snapshot`. The wizard's exit screens open `wa.me` with a prefilled text that names the situation (address already in Portugal, more than two adults, separate accounts, NIF already held) and adds "We also need NIFs for children." when that box was ticked (`src/components/apply/exit-screen.tsx`, `applyCopy.exits` in `src/content/apply.ts`); the visitor sends it. `whatsappMessage`, which wrote the applicant count, proof of address country and visa, is no longer called by any screen. The success page and the dashboard help box open WhatsApp with a generic help message. The sessionStorage keys are cleared when the order is saved or on "start over" (`src/components/apply/apply-wizard.tsx`); `alttavia_apply_checkout_v1` holds the product and the email the code was sent to (`checkout-storage.ts`).
- **Account.** "First name" and "Email address" in `src/components/auth/email-step.tsx`; the name goes to `public.users.full_name` after the code is verified (`src/components/apply/account-screens.tsx`). Both the application form and `/en/login` call `signInWithOtp` with `shouldCreateUser: true` (`email-step.tsx`, `code-step.tsx`, `login-flow.tsx`), so asking for a code creates the auth user, and the trigger in `0001_schema.sql` copies its email into `public.users`. `public.users.phone` exists, but no form fills it. No screen lets a client change their email, and the column grant in `0006_admin_hardening.sql` blocks it in `public.users`. [TO CONFIRM: whether email change is switched off in the Supabase Auth settings. If it is not, a signed in client could change it through the Auth API, and the trigger in `0001_schema.sql` would copy it to the profile.]
- **Payment.** `src/lib/stripe/checkout.ts` sends Stripe the account email, the order id as `client_reference_id`, and metadata (order id, user id, service slug). The order keeps `stripe_checkout_session_id`, `stripe_payment_intent_id`, `paid_at`, `total_cents`. No card data reaches our systems. While live mode has no price ids, the Payment Link fallback puts the email in the link's query string (`prefilled_email`).
- **Documents.** Live `service_docs` rows read on 2026-09-21. NIF only: passport, proof of address, NIF power of attorney. Bank Account only: passport, Portuguese tax identification number, tax identification number from your country, proof of address, bank statements or annual income statement, proof of profession, bank power of attorney. NIF + Bank Account and Couple package: passport, proof of address, tax identification number from your country, bank statements or annual income statement, proof of profession, both powers of attorney (couple: per applicant). PDF, JPEG or PNG, 10 MB each. R2 keys `orders/{orderId}/{docKey}/{applicant}/{uuid}.{ext}`. Every upload is a new row; rejected files and their rows stay. `user_documents` keeps the original file name as sent by the browser (sanitized), type, size, status, `rejection_reason`, `reviewed_at` and `reviewed_by` (`0001_schema.sql`); the reason is quoted in the rejection email. `user_service_events` records each stage move with its time and `actor_id` (`0001_schema.sql`, `src/lib/orders/lifecycle.ts`, `src/lib/orders/review.ts`). An upload whose confirm never arrives leaves a `pending` row, and the object if the upload itself went through; it stops blocking the slot after 15 minutes and nothing deletes it (`src/app/api/documents/upload-url/route.ts`).
- **Partner.** `user_service_applicants.applicant_index` and `user_documents.applicant_index` are 0 or 1 (`0007_one_unit_poa.sql`, `0001_schema.sql`); the account holder enters and uploads both. An order has two applicants for every joint account from the form (Couple package, joint Bank Account only, joint NIF + Bank Account: `applicantsFor` in `src/lib/apply/recommend.ts`) and for a Couple package bought from the client area's Services drawer (`src/app/api/orders/route.ts`). The form tells a couple that the partner's NIF is a second purchase from the dashboard (`secondNif` in `src/content/apply.ts`); on that order applicant 0 is the partner, the agreement prints applicant 0 (`src/content/contracts/variables.ts`) and is emailed to the account's address (`src/lib/contracts/ensure.ts`). That second order is written with `answers_snapshot: {}` and no questions (`src/app/api/orders/route.ts`), so partner answers exist only when a couple applies through the form (`src/app/api/apply/submit/route.ts`).
- **Children.** The form stores only `childrenNifs` as a boolean. `childrenHint` ("quoted on request after the adults' order") and `childrenNifs` ("Tell us how many on WhatsApp and we add them to the order") in `src/content/apply.ts`, and "Children's NIFs on request." in `src/content/bank-nif.ts`: any child's data reaches the firm outside the platform.
- **Details for documents.** `public.user_service_applicants` (nine fields, adults only: `src/lib/orders/applicant-rules.ts`). Powers of attorney are generated on demand and not stored (`GET /api/orders/[id]/poa/[docId]`); the signed copy is an ordinary upload. The service agreement PDF is stored in R2 under `contracts/{orderId}/v{n}.pdf` (earlier versions stay in the bucket when the firm regenerates), the printed values are copied into `user_service_contracts.variables`, and the PDF is emailed as an attachment through Resend (`src/lib/contracts/ensure.ts`). The optional signing place is `parseSigningPlace` in `src/lib/contracts/signing-place.ts`. The Couple package has no contract model (`services.contract_template` is null for it, `0009_service_contracts.sql`), so no agreement is prepared for it. "Edit your details" shows per deed slot while that slot accepts a file, so on a package it stays until both signed deeds are in and comes back after a rejection (`src/components/dashboard/documents/document-slot.tsx`); a correction after the agreement was prepared does not change the stored or emailed PDF until an admin regenerates it (`contractDrift` in `src/content/contracts/variables.ts`).
- **Deliverables.** `service_deliverables`: "Your Portuguese NIF", "Finanças access", "Your Portuguese IBAN"; files in R2 under `deliverables/{orderId}/`; closing report in `user_services.report`. An admin can remove a file delivered by mistake (`DELETE /api/admin/deliverables/[id]`, `deleteDeliverable` in `src/lib/orders/deliverables.ts`, commit `0a7cd67`).
- **Emails.** `src/lib/email/templates.ts` and `src/lib/orders/notify.ts` (commit `0a7cd67`). To the client: document rejected (with the team's reason), order completed (only the first time the order reaches its last stage, `firstCompletion` in `src/lib/orders/lifecycle.ts`), service agreement (PDF attached, `src/lib/contracts/ensure.ts`; sent again with every regenerated version) and payment received. To `EMAIL_TEAM_INBOX`: new paid order (service, amount, client email, order id), documents ready to review (service, client email, the number of applicants when there are two, the number of files to review, order id) and, from the Stripe webhook only, paid amount does not match the order (client email, both amounts, Stripe session id, order id). The last two carry the client's email in the subject. Admin feedback notes go to `FEEDBACK_TO` with the sender, the screen address and the text (`src/lib/email/feedback.ts`). The Feedback button prefills the screen address with its path and query (`src/components/admin/feedback-button.tsx`), which can hold a searched client email (`/admin/users?q=`) or an order or user id; the note is stored in `admin_feedback` (`0010_admin_feedback.sql`), and `/api/admin/feedback` has POST and PATCH only, no delete. Sign in codes: Supabase Auth through Resend SMTP from `hello@send.alttavia-relocation.com`. "Forgot your password?" on `/admin/login` calls `resetPasswordForEmail` for any typed address, with no role check (`src/components/admin/login-form.tsx`), so anyone with an account, a client included, can receive a password recovery code if their address is entered there; the recovery template carries the code (`scripts/auth-config.mjs`). Client replies go to `EMAIL_REPLY_TO`, a mailbox on alttavia-relocation.com [TO CONFIRM: mailbox provider]. `sendEmail` skips `.invalid` addresses (demo accounts). On staging `EMAIL_TEAM_INBOX` and `FEEDBACK_TO` are the developer's test inbox, business@guyshore.com; production values are set on September 24 and will be the firm's (owner, 2026-09-21).
- **Technical records.** Supabase Auth stores `auth.sessions.ip`, `auth.sessions.user_agent` and `auth.audit_log_entries.ip_address` (columns checked on 2026-09-21) in the project's own database. Nothing in `src/`, `scripts/` or `supabase/migrations/` clears them. [TO CONFIRM: how long Supabase itself keeps sessions and audit entries, and whether deleting an auth user removes its audit entries.] Netlify serves every page and runs every route handler, so it receives each visitor's IP address and browser, and holds the server logs. The browser also talks to Supabase directly (the sign in code request and its verification, and session refresh: `createBrowserClient` in `src/lib/supabase/client.ts`, `signInWithOtp` in `src/components/auth/email-step.tsx` and `code-step.tsx`) and to R2 directly (an upload is a PUT to a presigned URL, `src/components/dashboard/documents/document-slot.tsx`; a download redirects to one, `src/app/api/documents/[id]/route.ts` and `src/app/api/deliverables/[id]/route.ts`), so Supabase and Cloudflare receive the visitor's IP address and browser too. `sendEmail` logs the subject and the recipient when a send is skipped or fails (`src/lib/email/send.ts`), and some route errors log the user id (`src/app/api/apply/submit/route.ts`, `src/app/api/orders/route.ts`). Every admin download and every admin write logs one line with the admin id, the action and the row it touched (`audit` in `src/app/api/admin/_lib/http.ts`, plus the deed and agreement download routes). That is the only record of who opened a client's files; its period is Netlify's.
- **Access.** Row level security: clients read only their own rows. Admins read everything through `public.is_admin()`, which since `0011_admin_password_session.sql` also requires a session opened with a password; an admin signed in with an emailed code is a client (`src/lib/supabase/admin-user.ts`). No table accepts writes from the browser except a client's own `full_name` and `phone`. Two admin accounts on 2026-09-21: info@alttavia-relocation.com (Patrícia) and the support admin business+admin@guyshore.com, the developer's, created that day by `npm run admin:create -- --support` (`scripts/create-admin.mjs`), which the developer and the test scripts sign in with. It cannot simply be deleted once it has reviewed a document or moved an order: those rows point at it with no `on delete` rule (`0001_schema.sql`, `0005_admin.sql`), and `db:purge` never deletes an admin; removing its access means demoting it. Two factor sign in is planned for September 23 and enforced on September 24 (owner). Uploads and deliverables open through presigned links that last 120 seconds, for clients and admins alike (`PRESIGN_DOWNLOAD_SECONDS` in `src/lib/r2/client.ts`). The service agreement is not a presigned link: `GET /api/orders/[id]/contract` checks the session (owner or admin), reads the file with `getObjectBytes` and streams it, with no expiry and `Cache-Control: private, no-store`. The powers of attorney are generated on demand by `GET /api/orders/[id]/poa/[docId]` (owner or admin, paid orders only). The agreement also leaves storage as an email attachment. The R2 access key in `.env.local` reaches every object directly, and `npm run db:dump -- --with-files` copies them all (see Backups). An admin session needs a password, but "Forgot your password?" on `/admin/login` emails a recovery code that sets a new one (`src/components/admin/login-form.tsx`), so until two factor is enforced, access to an admin's inbox leads to the admin area.
- **Storage places.** Supabase project `dgdbrnvgrpixsslgvmns`, region eu-west-2 (London), in the guyshore.com organization. R2 bucket `alttavia-documents`, EU jurisdiction endpoint (`*.eu.r2.cloudflarestorage.com` in `.env.local`), in the developer's Cloudflare account; its CORS allows `localhost:3000`, the developer's LAN IP, production and staging. Netlify project `bank-and-nif-in-portugal` in the developer's team `guiblackelephant`; there is no `netlify.toml` in the repo, so the functions region is set in the Netlify dashboard. Production and the staging branch deploy (`staging--bank-and-nif-in-portugal.netlify.app`, Stripe in test mode) share the same Supabase project, Auth and R2 bucket, and so does the developer's local server (`.env.local`; `docs/platform-contract.md`, Staging). The Stripe account belongs to the firm; the developer has Developer access. Owner facts, 2026-09-21.
- **Backups.** `npm run db:dump` writes every public table and every auth user, and with `--with-files` every R2 object, to `%USERPROFILE%\alttavia-backups` on the developer's computer, outside the repo (`scripts/db-dump.mjs`, which itself warns that the folder holds personal data). Nothing uploads it elsewhere and nothing deletes old dumps. On 2026-09-21 that folder held two dumps, without files. `npm run db:restore -- --apply` puts back every missing row of an account that still exists (`scripts/db-restore.mjs`), so the records of deleted documents (file name, type, size, status, review) and deleted details can come back from an old dump. The files themselves do not: `db:restore` never uploads files, and they come back only if someone uploads them again by hand from a dump made with `--with-files`.
- **Deletion.** One route deletes client data: `DELETE /api/admin/deliverables/[id]` (admin only) removes a delivered file, then its row. Nothing else in `src/` deletes an account, answers, orders, uploads, details or agreements, and no client screen asks for it. `user_services.user_id` and `user_answers.user_id` are `on delete restrict` (`0004_hardening.sql`), so any account that finished the form has answers and an order and cannot be deleted until those are. By hand, the order is: the bucket prefixes `orders/{id}/`, `deliverables/{id}/`, `contracts/{id}/`; the orders (the database cascades events, uploads, details, agreement and deliverables); the answers; the auth user (cascades to the profile); then the backups. `npm run db:purge` does the same up to the auth user, but it chooses by kind, not by client (`scripts/purge-test-data.mjs`). It deletes demo accounts and the test accounts listed in the script. It also deletes every Stripe test mode order (`cs_test_`), whoever owns it, which can include a real client's order placed on staging, since staging shares the database. With `--i-know` it also deletes every unpaid order that has no payment session, whoever owns it, which the script itself warns may be a real prospect's. With each order go its events, uploads, details, agreement, deliverables, answers and bucket files. It never deletes an admin, it cannot pick one named client, and it never deletes backups. Deleting an order deletes its payment record, so keeping payment records means anonymizing the profile instead. There is no export of one client's data. [TO CONFIRM: the bucket has no lifecycle rule, a Cloudflare setting not recorded in the repo.]
- **Controller and identity.** `src/content/contracts/models.generated.ts`: the Second Party is ALTTAVIA RELOCATION, Unipessoal Lda., NIPC 518856984, registered office Av. Elias Garcia, 123-A (also in Annex I, Part C); First Clause, 2 has the legal services rendered by Patrícia Soares Viana through the company on an independent basis. `src/content/power-of-attorney.ts`: the deeds appoint her personally, at her professional address Av. António Augusto Aguiar, 24, 1.º Direito, Escritório 3, with power to substitute and, for a NIF, as tax representative. `CONTRACT_LETTERHEAD` (her OA email, visas.vianaconsultancy.com, the Aguiar address) is not drawn on the PDF. `src/content/brand.ts` (`legalEntity: "Viana Consultancy"`), `src/components/bank/site-footer.tsx`, `src/components/bank/structured-data.tsx`. Stripe's public name "Consulting": owner.
- **Cookies.** `@supabase/ssr` 0.12.7 defaults: path `/`, SameSite Lax, not HttpOnly, max age 400 days; the browser client uses the PKCE flow. The installed `@supabase/auth-js` 2.116 writes three cookies on each code request: a per flow slot `…-auth-token-flow-<id>-code-verifier`, an index `…-auth-token-flows-code-verifier` and the legacy `…-auth-token-code-verifier` (`node_modules/@supabase/auth-js/dist/main/lib/helpers.js`, `storePKCEVerifier`). `verifyOtp` does not remove them; `removeAllPKCEVerifiers` runs on session teardown, such as sign out (`GoTrueClient.js`, `_removeSession`), and `@supabase/ssr` gives them its 400 day max age. Evidence from the installed library: check the cookie jar in a browser on staging before publishing. `src/proxy.ts` refreshes the session on each request; sign out clears it (`src/app/api/auth/signout/route.ts`). `alttavia_locale` is read by `src/app/page.tsx`, but no live page sets it (only the unused `src/components/sections/navbar.tsx` renders the switcher that writes it). GTM: `src/components/analytics.tsx`, mounted on every page by `src/app/layout.tsx`, loaded only when `NEXT_PUBLIC_GTM_ID` is set; it is empty in `.env.local` and not set on any deploy (owner, 2026-09-21). As soon as it is set, GTM loads for every visitor with no consent step, and there is no cookie banner: do not set it before a consent banner exists, or the promise in the notice breaks. Fonts: `next/font/google` in `src/app/layout.tsx`, self hosted at build time (`node_modules/next/dist/docs/01-app/03-api-reference/02-components/font.md`: no requests are sent to Google by the browser). The landing has no embedded third party content; press and review entries are plain links.
